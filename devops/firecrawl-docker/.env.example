# ===== Required =====
PORT=3002
HOST=0.0.0.0
USE_DB_AUTHENTICATION=false

# Change this — protects the Bull queue admin UI
BULL_AUTH_KEY=changeme-set-a-real-key

# ===== AI Features (optional) =====
# Needed for: /extract API, JSON format on scrape, summary/branding formats.
# Basic scraping and crawling work without any AI key.

# Option A: OpenAI (recommended — natively supported)
# OPENAI_API_KEY=sk-...

# Option B: Gemini via OpenAI-compatible endpoint (experimental)
# OPENAI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
# OPENAI_API_KEY=<your-gemini-api-key>

# ===== Auto-configured by docker-compose (do not change) =====
# REDIS_URL=redis://redis:6379
# REDIS_RATE_LIMIT_URL=redis://redis:6379
# PLAYWRIGHT_MICROSERVICE_URL=http://playwright-service:3000/scrape
