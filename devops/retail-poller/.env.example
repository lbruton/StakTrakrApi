# StakTrakr Retail Poller — environment variables
# Copy to .env and fill in values before starting the container.

# ── Git credentials ─────────────────────────────────────────────────────────
# GitHub Personal Access Token with 'repo' scope (for git push origin data).
# Create at: https://github.com/settings/tokens
GH_TOKEN=

# ── Data branch worktree ─────────────────────────────────────────────────────
# Absolute path on the HOST machine to a git worktree of the data branch.
# One-time setup:
#   git -C /path/to/StakTrakr worktree add ~/staktrakr-data data
# Then set this to the absolute path (no ~ expansion in docker-compose):
DATA_REPO_HOST_PATH=/Users/you/staktrakr-data

# ── Firecrawl ────────────────────────────────────────────────────────────────
# Set automatically to http://api:3002 by docker-compose (the firecrawl 'api'
# service on the same network). Override here only to point at a different endpoint.
# FIRECRAWL_BASE_URL=http://api:3002

# ── Playwright / Browserless fallback ────────────────────────────────────────
# WebSocket endpoint for the self-hosted browserless Docker container.
# Used as a fallback when Firecrawl cannot extract a price from a JS-heavy page.
# Default for the local browserless stack (devops/browserless/):
#   BROWSERLESS_URL=ws://host.docker.internal:3000/chromium/playwright?token=local_dev_token
# Leave unset to disable Playwright fallback (Firecrawl-only mode).
BROWSERLESS_URL=ws://host.docker.internal:3000/chromium/playwright?token=local_dev_token
