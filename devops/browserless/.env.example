BROWSERLESS_TOKEN=local_dev_token

# Timezone for the container (default: America/New_York)
# TZ=America/New_York

# Optional: mount a local repo root for file:// protocol access in Playwright tests.
# Set to the absolute path of the StakTrakr repo on this machine.
# macOS:  WORKSPACE_DIR=/Volumes/DATA/GitHub/StakTrakr
# Ubuntu: WORKSPACE_DIR=/home/user/StakTrakr
# Leave unset (or omit) for pure CDP/scraping mode — no file:// access needed.
# WORKSPACE_DIR=
